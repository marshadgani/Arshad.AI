import ComingSoonPage from '../components/ComingSoonPage';

export default function ActivityLog() {
  return (
    <ComingSoonPage
      title="Activity log"
      emoji="📜"
      reason="Activity logging is planned but not yet running — your actions are not being captured or stored anywhere today. This page will show provider activity (calendar, email, GitHub) once the audit-log backend exists."
    />
  );
}
