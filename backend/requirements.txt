# Pinned for reproducible builds. Update via:
#   pip install --upgrade <pkg> && pip freeze > requirements.txt
# then verify the diff before committing.

# fastapi/authlib/cryptography/pyjwt/python-dotenv bumped 2026-09-07 — SEC-001,
# pip-audit found 41 known CVEs across these pins (pyjwt alone: 11 open
# advisories on the exact jwt.decode() path every authenticated request hits).
# fastapi>=0.133.0 is the minimum that pulls a fixed starlette (>=1.3.1)
# without an explicit override.
fastapi==0.133.0
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
redis==5.2.1
anthropic==0.42.0
python-dotenv==1.2.2
pydantic[email]==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
alembic==1.14.0
httpx==0.28.1

# Phase C — OAuth + JWT auth
authlib==1.6.12
cryptography==49.0.0
pyjwt==2.13.0
