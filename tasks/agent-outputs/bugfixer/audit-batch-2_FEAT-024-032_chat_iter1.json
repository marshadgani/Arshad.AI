{
  "audit_run_id": "audit-2026-04-26",
  "batch": 2,
  "iteration": 1,
  "verdict": "applied 2 fixes; 0 remaining real defects",
  "fixes_applied": [
    {
      "defect_id": "DEF-028-01",
      "file": "backend/src/services/chat.py:389-403",
      "summary": "Wrapped final assistant_text persistence in try/except GeneratorExit so partial text survives client disconnect mid-stream. The user message was already committed pre-stream (line 277). Now if the consumer drops, the GeneratorExit handler best-effort persists a row with {text:..., _partial:true} marker before re-raising. Best-effort: catches any inner exception silently to avoid masking the original cancellation.",
      "verification": "Boot-tested: src.main imports cleanly; TestClient(/health)=200; new try/finally surrounds final commit + [DONE] yield."
    },
    {
      "defect_id": "DEF-032-01",
      "file": "backend/src/services/chat.py:285-294",
      "summary": "Removed `intent != 'general'` guard that suppressed _build_tool_schemas. New rule: tool_schemas=None ONLY when both tool_names AND agent_slugs are empty. Council chairman (the sole agent on general intent) is now correctly exposed.",
      "verification": "_tool_subset('general') returns ([], ['ai_core_council_chairman']); _build_tool_schemas() now produces 1 schema named 'agent_ai_core_council_chairman' for general intent (was None before)."
    }
  ],
  "untestable_in_static_review_carried_forward": [
    {"defect_id": "DEF-026-01", "test_id": "TC-026-08", "note": "250-msg pagination requires live DB seed"},
    {"defect_id": "DEF-028-02", "test_id": "TC-028-09", "note": "live SSE header inspection"},
    {"defect_id": "DEF-030-01", "test_id": "TC-030-09", "note": "SDK error propagation needs mock harness"}
  ]
}
