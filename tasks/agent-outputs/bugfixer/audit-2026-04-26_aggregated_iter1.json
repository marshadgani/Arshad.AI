{
  "audit_run_id": "audit-2026-04-26",
  "iteration": 1,
  "fixes_applied": [
    {
      "defect_id": "DEF-100-01",
      "feature_id": "FEAT-100",
      "file": "backend/src/integrations/personal/oauth_providers.py",
      "lines": "151-168",
      "before": "sync = make_oauth_sync_via_api(sync_url='https://api.ouraring.com/v2/usercollection/daily_readiness?start_date=2026-04-20', ...)",
      "after": "Override sync as async method that computes start_date as (today - 7 days).isoformat() at call time. Builds the factory call with the dynamic URL inside the method body, then invokes runner(self, ...).",
      "rationale": "Hardcoded date would return ever-growing windows of readiness data over time. Now always returns last 7 days. Bonus: stores window_start in integration.config for observability."
    },
    {
      "defect_id": "DEF-112-01",
      "feature_id": "FEAT-112",
      "file": "backend/src/integrations/project/_factory.py",
      "lines": "114-117",
      "before": "integration.config = (spec.parse_sync(body) if spec.parse_sync else {'ok': True})",
      "after": "integration.config = {**(integration.config or {}), **(spec.parse_sync(body) if spec.parse_sync else {'ok': True})}",
      "rationale": "Aligns project factory with personal-provider behaviour: sync now MERGES into existing config rather than replacing. Preserves connect-time probe data and any other connect-time state across syncs."
    }
  ],
  "verification": "Both fixes import cleanly. OuraIntegration.sync is now a bound method; factory _factory.py make_provider returns class whose sync uses spread-merge. Smoke test confirmed.",
  "remaining_defects": []
}
