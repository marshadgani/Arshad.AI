# Dev-Team Pipeline Queue

> Tracks every feature/change/bug that has gone through, or is waiting to go through,
> the `.claude/workflows/dev-team-pipeline.js` Workflow. Source of truth for resuming
> the pipeline in a NEW session — `resumeFromRunId` only works within the session that
> created the run, so a fresh session re-seeds from the artifacts referenced here
> instead of replaying already-completed stages.

## How to use this file (for Claude, every session)

1. On any user message describing a change, new feature, or bug — assign the next
   `FEAT_ID` from `tasks/.feature-counter` (increment it), and append a row below
   with `status: queued`.
2. If a workflow run is already active THIS session, resume it:
   `Workflow({ scriptPath: ".claude/workflows/dev-team-pipeline.js", resumeFromRunId: "<run_id>", args: { features: [...all queued + in-flight features...] } })`
   — completed features return instantly from cache; the new one interleaves.
3. If this is a NEW session (no active run_id), start a fresh run with `args.features`
   containing only the features still `queued` or `in_flight` in this file. Do NOT
   re-include `completed` features as full entries — their code is already committed;
   there's nothing to resume for them.
4. After each run completes (success, halt, or error), update this file: move the
   row to `completed`/`halted`/`error`, record the branch name and EA decision.
5. Never run two Workflow invocations concurrently against the same run_id from two
   different conversation turns — check `/workflows` or this file's `active_run_id`
   field first.
6. **Never launch a second, separate `Workflow` run while one is still active**, even
   for unrelated new features — the per-role mutex lives in that run's own script
   state and does NOT extend across separate invocations. If a run is active and a
   new feature comes in, add it to the Queue table as `queued` and either wait for
   the active run to settle before launching a combined resume, or let it sit for
   the hourly Routine to pick up together with whatever's still pending then.

## Active run

| Field | Value |
|---|---|
| active_run_id | none — `wf_f057305c-e52`'s resume (task `w8729fdti`) was killed by a container restart on 2026-09-14 before reporting a result; work is lost, not resumable (`resumeFromRunId` only works within the session that created the run, and the crash happened inside that same session). FEAT-148/153/155's disk output was already committed to `dev-team/feat-143-155-resume-wip` (unreviewed) before the restart hit — see Queue table. Re-launch as a fresh run if these three need finishing. |
| session | session_01S3fJzZzJykfHRA9jhMycuk |
| started | 2026-09-13 (this run) |
| FEAT-142 (OAuth invalid_state bug) | Settled 2026-09-13, second pass, 30/30 stages, EA SHIP, security PASS. Committed to `dev-team/feat-142-oauth-cookie-scope-fix`. Blocked only on a Vercel env var (`VITE_API_BASE_URL`) that Arshad needs to set manually — no MCP tool available to do it. |
| portal-audit + ontology batch | Run `wf_c61e8300-546` (final task `w3d93hhub`) settled 2026-09-13 — **mixed result**: FEAT-136/137/138 completed and pushed as branches; FEAT-139/140/141 halted. See Queue table for per-feature outcomes and reasons. Halted features' in-progress working-tree state was preserved (unmerged) on `dev-team/feat-139-141-halted-wip` — do not merge until reviewed. |
| 15-feature batch | Settled 2026-09-07 — ALL halted. See Queue table for per-feature reasons. |
| convergence-test batch | Task `wm5xz1tv5` (FEAT-119 + FEAT-121, redesign-loop prompt fix) settled 2026-09-07 — **mixed result**: FEAT-119 got past Architecture Critic entirely (redesign fix worked). FEAT-121 is still Architecture-Critic-blocked with fresh findings even after the fix — this one needs a human decision, not another auto-iteration (see FEAT-121 row + notes below). |
| resume batch | Task `w9kn2psyx` (FEAT-119 + FEAT-120) completed 2026-09-11. Both, plus FEAT-118 (fixed directly earlier, riding along on the same branch), went through code-reviewer/security-auditor/test-writer/etc., accumulated as commits on `claude/ai-personal-assistant-CcA11`, gated via the full 8-agent "Merge to Main" panel, and **merged to `claude/ai-personal-assistant-main` via PR #71 on 2026-09-12** (squash commit `b348e79`). Deployed to Render — confirmed healthy (Application startup complete, `/health` 200s). FEAT-121 remains excluded, still awaiting a human decision on scope/approach. |

*(A run_id is only valid within the session that created it. If you're in a different session, ignore this field and start fresh per step 3 above — then update this table.)*

## Always-on continuation (standing Routine)

Per CLAUDE.md's "🔁 Always-On Pipeline" policy, this queue is checked and driven
forward automatically, not just when Arshad is actively chatting:

| Field | Value |
|---|---|
| routine_id | trig_018w2XJ9mHBfqUZ1uciMNqit |
| name | Dev-Team Pipeline Continuation |
| schedule | hourly (`create_new_session_on_fire: true`) |
| created | 2026-09-06 |

If this routine is ever missing (check `list_triggers`), recreate it — see CLAUDE.md.
Every firing reads this file, resumes/restarts any `queued`/`in_flight`/`error`
feature's Workflow run, and updates this file when it settles. It no-ops if the
queue below is empty.

## Queue

**2026-09-07 — the 15-feature combined run (`wf_14899418-22d`) completed. Every
single feature halted — none shipped.** Root causes: FEAT-118 hit a real
pre-existing security escalation (fixed directly, see below); 11 features hit
Architecture Critic still blocking after 2 redesign rounds (investigating —
found the redesign prompt never told Solution Architect to re-read the actual
cited files or patch its previous SDD instead of regenerating blind; fixed and
testing convergence on a small batch now); FEAT-120 hit a real denylist
violation (kept per decision, see notes); FEAT-126/130 halted earlier at EA
pre-build with resubmission checklists.

| FEAT_ID | Requirement (short) | Status | Branch | EA Decision | Notes |
|---|---|---|---|---|---|
| FEAT-118 | Chat window blocks screen (redesign) + app-wide mobile responsiveness | completed | `claude/ai-personal-assistant-CcA11` → merged via PR #71 | SHIP | Reached EA post-build, halted only on Security Auditor escalations (SEC-001 dependency CVEs, SEC-002 OAuth login CSRF), both fixed directly (commit `66cde24`). Re-verified clean by the full 8-agent "Merge to Main" gate on 2026-09-11/12 (code-reviewer/security-auditor/debugger/test-writer/refactorer/doc-writer/silent-failure-hunter/pr-test-analyzer all PASS or resolved WARN). **Merged to main 2026-09-12** (squash `b348e79`), deployed to Render, confirmed healthy. |
| FEAT-119 | Shopify integration — live store confirmed to have real orders. | completed | `claude/ai-personal-assistant-CcA11` → merged via PR #71 | SHIP | Was Architecture Critic-blocked (broken `connect()` signature, unpersisted `shop_domain`, timezone/throttling bugs) even after 2 redesign rounds. Convergence test (`wm5xz1tv5`) got it past Architecture Critic; task `w9kn2psyx` carried it through the rest of the pipeline. Gate found 2 real correctness bugs (unquoted Shopify date-query timestamps, an uncaught-500 path breaking the dashboard's always-200 contract) and a 0%-coverage gap — both fixed, coverage closed to 85-100%. **Merged to main 2026-09-12** (squash `b348e79`). |
| FEAT-120 | Whoop + Apple Health for Health & Fitness. | completed | `claude/ai-personal-assistant-CcA11` → merged via PR #71 | SHIP | Denylist violation: an agent wrote `backend/src/middleware/rate_limit.py` directly via its own Write tool. Content reviewed — safe, a legitimate Whoop-rate-limiter extraction shared with the Apple Health ingest route. **Kept per explicit decision** (commit `edccc1f`, `DENY_EXCEPTIONS`); the denylist enforcement gap itself remains a known unresolved issue (script has no filesystem access to verify actual disk state — see note below). Gate found and fixed a Critical bug (`close_whoop_client` NameError on every shutdown) and closed a coverage gap on the Whoop pooled client. **Merged to main 2026-09-12** (squash `b348e79`), deployed, confirmed healthy. |
| FEAT-121 | Google Calendar auto-booking + time audit + Spotify focus-telemetry. | halted | — | — | Was Architecture Critic-blocked (crash-recovery-impossible schema, false-positive leaked-time detection, unstable evidence timestamps) even after 2 redesign rounds. **Convergence test (`wm5xz1tv5`) did NOT resolve it** — still blocked with fresh findings after a 3rd redesign pass using the improved prompt (Solution Architect did read the cited files this time, so the fix is working as intended — the design is just genuinely hard). Deliberately **excluded** from the `w9kn2psyx` resume. Needs Arshad's decision: (a) allow more redesign iterations past the current cap, (b) reduce scope (e.g. drop crash-recovery guarantees or the leaked-time detector to a simpler heuristic), or (c) give the Solution Architect explicit design guidance up front. Not auto-retried further until decided. |
| FEAT-122 | Gmail universal ingest bus + dropped-ball detector. | halted | — | — | Architecture Critic still blocking after 2 rounds (non-functional retry logic in `_retry.py`). Awaiting the convergence-test result before relaunching. |
| FEAT-123 | Google Drive permissions audit + knowledge_suggestions. | halted | — | — | Architecture Critic still blocking after 2 rounds (Alembic `down_revision` doesn't point at the actual head — verified against the real migration file). Awaiting the convergence-test result. |
| FEAT-124 | Zoom promise ledger + Obsidian filing. | halted | — | — | Architecture Critic still blocking after 2 rounds (new DAG ids never registered in the ingestion runner — dead code path). Awaiting the convergence-test result. |
| FEAT-125 | Shopify intelligence layer (builds on FEAT-119). | queued | — | — | FEAT-119 now shows `completed` (merged to main) — **unblocked, eligible for the next batch**. |
| FEAT-126 | Zomato pre-staged cart + spend tracking + burnout chart (lowest priority). | halted | — | — | Halted earlier than the others — **EA pre-build rejected** (not Architecture Critic) with a resubmission checklist: resolve domain placement (calendar vs. new 'lifestyle' domain), inherit the standard OAuth base class, add missing FKs, route through `services/gateway.py`, use an ingested_* table naming pattern, and drop the burnout chart into its own separate FEAT entirely. Needs the BPDD itself revised, not just a code redesign — different fix path than the Architecture-Critic-blocked features. |
| FEAT-127 | Figma token-drift check + auto-diagrams. | halted | — | — | Architecture Critic still blocking after 2 rounds — ironically for building the Figma substrate anyway despite the requirement explicitly saying to flag back instead of building against nothing. Awaiting the convergence-test result. |
| FEAT-128 | Canva product creatives + monthly one-pager. | halted | — | — | Architecture Critic still blocking after 2 rounds (Shopify API version pinned outside its support window). Awaiting the convergence-test result. |
| FEAT-129 | Booking.com trip-feasibility checker (builds on FEAT-125). | queued | — | — | Depends on FEAT-125 — **do not launch until FEAT-125 shows `completed`**. |
| FEAT-130 | Calendly dynamic availability + single-use links + no-show scoring. | halted | — | — | Halted at **EA pre-build** (non-blocking refinement items: rate limiting on the public booking endpoint, URL-encoding edge case, a deployment-doc note, a dedup/idempotency requirement for the no-show DAG). Same different-fix-path note as FEAT-126 — revise the BPDD, not a code redesign. |
| FEAT-131 | Uber cost-of-attendance annotation + leave-by alarm. | halted | — | — | Architecture Critic still blocking after 2 rounds (`CREATE INDEX CONCURRENTLY` inside an Alembic migration — will fail at deploy since Alembic wraps migrations in a transaction). Awaiting the convergence-test result. |
| FEAT-132 | Render per-deploy regression verdict. | halted | — | — | Architecture Critic still blocking after 2 rounds (log level field discarded before the classification step that needs it). Awaiting the convergence-test result. |
| FEAT-133 | Supabase branch-per-feature + advisors-to-backlog. | halted | — | — | Architecture Critic still blocking after 2 rounds (step ordering contradicts the actual orchestrator.md sequence). Awaiting the convergence-test result. |
| FEAT-134 | Vercel toolbar-comment-to-pipeline bridge. | halted | — | — | Architecture Critic still blocking after 2 rounds ("poll get_runtime_errors" has no actual poller wired up). Awaiting the convergence-test result. |
| FEAT-135 | Coursera — verify personal progress data before building `/learning`. | queued (blocked) | — | — | Still gated on Arshad's reconnect — not launched. |
| FEAT-136 | Wire Dashboard's Tasks/Decisions/Notifications/Agent-Activity widgets to real `ingested_gmail_threads`/`ingested_github_activity` tables instead of the one-time `seed_from_mock.py` rows. | completed | `dev-team/feat-136-wire-dashboard-decisions-widget-to-live-data` | approved | Pushed to origin. Not yet merged to `claude/ai-personal-assistant-main` — needs its own "Merge to Main" gate cycle (8-agent panel), separate from the fetch-github-repo merges. |
| FEAT-137 | Surface real Upstox/Zerodha holdings on Personal Finance and Stock Market pages. | completed | `dev-team/feat-137-surface-real-broker-holdings-upstox-zerodha-kite-` | approved | Pushed to origin, stacked on top of FEAT-136's branch. Awaiting its own Merge-to-Main gate. |
| FEAT-138 | Wire Dashboard weather widget to the already-connected OpenWeatherMap provider. | completed | `dev-team/feat-138-feat-138-wire-dashboard-weather-widget-to-openweat` | approved_with_caveats | Pushed to origin, stacked on top of FEAT-137's branch. EA approved with caveats — read the caveats in this branch's own agent outputs/EA sign-off before merging; awaiting its own Merge-to-Main gate. |
| FEAT-139 | Add a GitHub activity feed/widget (issues/PRs) fed by `ingested_github_activity`. | halted | — | — | **Halted: unresolved Security Auditor escalation.** Needs Arshad's review before any retry — per the Always-On Pipeline rule, halts are not auto-retried. In-progress working-tree state (github_mapping.py/github_store.py/github_outcome.py etc.) preserved unmerged on `dev-team/feat-139-141-halted-wip`. |
| FEAT-140 | Relabel Home & IoT, Learning, and Travel pages as "Coming soon" (matching the existing honest `coming_soon.py` pattern for WhatsApp/Instagram/etc.), removing their fabricated static KPI/data displays. | halted | — | — | **Halted: pipeline warning — "added_at timestamp not provided"** (looks like a minor process-organiser/logging defect in the pipeline script itself, not a real architectural or security problem — worth a quick look before re-running). In-progress working-tree state (comingSoonDomains.ts, ComingSoonPages.test.tsx, etc.) preserved unmerged on `dev-team/feat-139-141-halted-wip`. |
| FEAT-141 | Turn Obsidian into a "second brain" that mirrors all data flowing into the Arshad.AI portal, organised as an **ontology layer** — not flat per-record notes, but linked entities and relationships (people, projects, threads, events, holdings, etc.) with tags and Maps-of-Content (MOCs) tying domains together, so the vault reads as a navigable knowledge graph rather than a data dump. | halted | — | — | **Halted: unresolved Security Auditor escalation**, and separately a Bug Fixer iteration 4 hit the 32000-output-token API limit mid-fix (not itself a design problem, but worth noting if retried — the fix content may need to be split smaller). In-progress working-tree state (`backend/src/models/ontology.py`, `frontend/src/types/ontology.ts`, `useOntologyEntities.ts`, `useOntologyStatus.ts`, `useTriggerOntologySync.ts`, the ontology Alembic migration, plus doc updates to `.claude/rules/api.md` and `CLAUDE.md`) preserved unmerged on `dev-team/feat-139-141-halted-wip`. Needs Arshad's review of the security finding before any retry. |
| FEAT-142 | Bug: login OAuth callback fails with `invalid_state — "OAuth state does not match this browser session."` on `arshad-ai.onrender.com`. | completed | `dev-team/feat-142-oauth-cookie-scope-fix` | approved (SHIP) | Second pipeline pass (run `wf_43aa76ac-55d`, scoped to the confirmed frontend-only fix) went all 30 stages clean — no denylist violation this time. Enterprise Architect post-build: **SHIP**. Security Auditor: **PASS**, 2 real findings (open-redirect/`javascript:` URI validation on the backend-origin input; plaintext-`http://` rejected in production) both fixed in place; 27 tests added/passing, `tsc` clean. Committed to its own branch, pushed, **not yet merged to `claude/ai-personal-assistant-main`** — needs its own Merge-to-Main gate cycle. **Deploy blocker (why the pipeline itself reported "halted" before this was resolved): `VITE_API_BASE_URL` must be set to `https://arshad-ai.onrender.com` in the Vercel project's env (Production + Preview scopes) before this fix can work — Vite inlines the value at build time, so setting it after deploy has no effect until a rebuild is triggered. No MCP tool in this session can set Vercel env vars — Arshad must do this manually in the Vercel dashboard, then trigger a redeploy (or it'll pick it up on the next deploy once this branch reaches main).** Post-deploy, verify per CLAUDE.md §23 that a real login produces no `invalid_state` line in Render logs. |
**ID collision note (resolved during the 2026-09-14 merge from main):** this session assigned FEAT-143/FEAT-144 to the GitHub-Connect and Sync-now bugs below, incrementing `tasks/.feature-counter` from 142 without knowing a concurrent session had independently assigned the same two IDs to an Arshad-directed "UI Design Council" run — which shipped and merged to main first (PR #86/#87). Since this session's FEAT-143/144 never shipped (halted/WIP-only), they're renumbered to FEAT-156/FEAT-157 below to avoid a permanent duplicate-ID collision in history. `tasks/.feature-counter` reset to 157 to reflect this.

| FEAT-156 | Bug: GitHub/Gmail/Google Calendar/Drive/Tasks/YouTube "Connect" silently bounces back to dashboard for an already-authenticated user. Root cause: `backend/src/integrations/personal/_shared.py`'s `upsert_personal_integration()` only checks for a pre-existing `oauth_account` row created at login — if absent, it redirects to `FRONTEND_URL/login?reason=missing_github_scope`, but `App.tsx`'s `LoginRoute` guard sees a valid token and immediately redirects back to `/`, discarding the `reason` param. Need a real "connect an additional provider while already logged in" OAuth path. | queued | — | — | Found in 2026-09-14 code-based audit (5 parallel Explore agents), reported by Arshad via screenshot ("GitHub Connect just loads and bounces back to dashboard"). Originally assigned FEAT-143 — renumbered, see ID collision note above. |
| FEAT-157 | Bug: Google Calendar/Gmail/GitHub "Sync now" reports success but does nothing. Root cause: `make_sync_via_dag()` in `_shared.py` only inserts a row into `dag_trigger_queue` and immediately returns a success `SyncResult(rows_written=0, ...)` — no actual API call happens synchronously, and real ingestion only runs if `queue_worker.py` is running (gated behind `ENABLE_INPROCESS_WORKER=true`) or Airflow is draining the queue (Render doesn't run Airflow). Need to confirm `ENABLE_INPROCESS_WORKER` is set in Render prod, and/or make the sync endpoint reflect real enqueue-vs-processed state instead of a false success. | queued | — | — | Found in 2026-09-14 code-based audit, reported by Arshad via screenshot ("Sync now, but I don't think anything is getting synced"). Originally assigned FEAT-144 — renumbered, see ID collision note above. |
| FEAT-145 | `IntegrationProvider.disconnect()`'s default in `backend/src/integrations/base.py` only flips status — it does not revoke credentials with the third-party provider or scrub stored tokens, for every provider except Apple Health. The UI's disconnect confirmation dialog promises "Stored credentials will be removed" for all of them — a false promise. Fix the default (or each provider override) to actually revoke/delete credentials on disconnect. | queued | — | — | Found in 2026-09-14 code-based audit. |
| FEAT-146 | Slack integration's `parse_probe`/`parse_sync` in `backend/src/integrations/project/bulk_providers.py` raise a bare `Exception` on failure instead of `IntegrationError`; `_factory.py`'s error handling only catches `IntegrationError`/`httpx.HTTPError`, so a Slack failure propagates as an unhandled 500 instead of a clean integration-error response. | queued | — | — | Found in 2026-09-14 code-based audit. |
| FEAT-147 | Complete the Dashboard live-data work: re-land FEAT-136 (Tasks/Decisions/Notifications/Agent-Activity widgets → real ingested tables) and FEAT-137 (broker holdings) properly on main, fix `QuickActionsCard.tsx` and `KnowledgeSearchCard.tsx` (zero `onClick`/`onChange`/`onSubmit` handlers anywhere), fix `FocusCard.tsx`'s dead action button, and remove or make honest `AgentActivityCard.tsx`'s `live` prop (shows a pulsing "live" dot over static seeded data). | queued | — | — | Found in 2026-09-14 code-based audit. Supersedes/completes stalled FEAT-136/137 (those branches exist unmerged — reuse their code where still valid rather than regenerating from scratch). |
| FEAT-148 | `frontend/src/components/DomainPage.tsx`'s "Open →" button and "Configure" button have zero `onClick` handlers — dead UI on every generic domain page. | queued | — | — | Found in 2026-09-14 code-based audit. |
| FEAT-149 | Correctly relabel Home & IoT, Learning, and Travel pages as "Coming soon" (re-do FEAT-140, which halted on a pipeline logging defect and whose relabeling never actually touched `HomeIoT.tsx`/`Learning.tsx`/`Travel.tsx` — confirmed via near-empty git log for those files). | queued | — | — | Found in 2026-09-14 code-based audit (git-log verification). Supersedes stalled FEAT-140. |
| FEAT-150 | Wire real Upstox/Zerodha broker holdings into `PersonalFinance.tsx` and `StockMarket.tsx` pages directly (FEAT-137 only reached the Dashboard widget). Restore `backend/src/api/v1/finance.py` (currently only a stale `.pyc` exists, not imported in `main.py`) or design its real replacement. | queued | — | — | Found in 2026-09-14 code-based audit. Depends conceptually on FEAT-137/147's broker-sync code — batch after FEAT-147, not concurrently. |
| FEAT-151 | `frontend/src/components/FundFlowMap/FundFlowMap.tsx` is a 100% hardcoded SVG string (`MAP_SVG`) rendered via `dangerouslySetInnerHTML`, with zero data binding to real fund-flow data. Either wire it to real data or relabel it honestly as illustrative/coming-soon. | queued | — | — | Found in 2026-09-14 code-based audit. |
| FEAT-152 | `frontend/src/components/TopBar.tsx`: notifications bell, settings gear, profile avatar, and Quick Capture input are all dead/decorative with no handlers. Wire each to a real destination/action or remove it. | queued | — | — | Found in 2026-09-14 code-based audit. |
| FEAT-153 | `frontend/src/components/Sidebar.tsx`: "Settings" and "Activity log" links are both `href="#"` with no matching route. Add the routes or remove the links. | queued | — | — | Found in 2026-09-14 code-based audit. |
| FEAT-154 | `backend/scripts/register_skills.py` is never invoked automatically anywhere (no CI/Docker/post-migration hook) — the AI Ecosystem page's Skills tab stays empty despite 347 real skills on disk. Add an automated invocation (e.g. on deploy or via a lightweight startup hook). | queued | — | — | Found in 2026-09-14 code-based audit. |
| FEAT-155 | Small hygiene batch: fix Shopify integration's `sync()` (`backend/src/integrations/personal/shopify.py`) which only refreshes shop metadata despite the UI description promising "Live orders, revenue, and inventory"; validate the Plaid `api_key` direct-paste path (`backend/src/integrations/personal/plaid.py`) which currently stores an unvalidated token; clean up stale/misleading comments found during the audit. | queued | — | — | Found in 2026-09-14 code-based audit. Lowest priority — batch last. |

**Cross-feature dependencies — the script has no ordering between features in one batch, so this must be managed by NOT batching dependents with their prerequisites:**
- FEAT-125 (Shopify intelligence layer) needs FEAT-119 (base Shopify integration) to be `completed` first — its Solution Architect stage would otherwise design against Shopify code that doesn't exist yet. **Do not include FEAT-125 in the same `args.features` batch as FEAT-119.**
- FEAT-129 (Booking.com trip-feasibility) needs FEAT-125's runway data — same rule, one batch later again.
- Everything else in this queue (121-124, 126-128, 130-134) has no dependency on another queued feature and can go in the very next batch.
- FEAT-136/137/138/139/140 (the 2026-09-12 portal live-data audit fixes) have no dependency on each other or on any other queued feature — safe to batch together, and safe to add to any other batch alongside them.
- FEAT-156/157/145/146/148/149/151/152/153/154/155 (the 2026-09-14 audit findings; FEAT-156/157 were originally 143/144, renumbered — see ID collision note above) have no dependency on each other or on any other queued feature — safe to batch together.
- FEAT-147 (Dashboard live-data completion) should go in the SAME or an EARLIER batch than FEAT-150 (broker holdings on Finance/Stock pages) since FEAT-150 reuses FEAT-147's broker-sync code — do not put FEAT-150 in an earlier batch than FEAT-147.

**Launch plan for the next batch (FEAT-118/119/120/142 are done and merged — no need to include them):** FEAT-156, FEAT-157, and FEAT-145 through FEAT-149 and FEAT-151 through FEAT-155 (2026-09-14 audit batch, Arshad-directed "go page by page, click every button" audit — FEAT-156/157 were originally 143/144, renumbered) are the priority batch — start these first, launched together since none of them depend on each other. FEAT-150 follows in the same or a later batch, after FEAT-147. FEAT-125 is now unblocked (FEAT-119 completed) and can go in the same or a later batch as the still-halted Architecture-Critic features (122, 123, 124, 127, 128, 131, 132, 133, 134), all relaunched with the fixed redesign-loop prompt. FEAT-126 and FEAT-130 need their BPDD revised first (see their notes), not a redesign-loop retry — a different fix path, don't include them in a redesign-loop batch. FEAT-121 stays excluded pending Arshad's decision (see its row). FEAT-129 stays queued behind FEAT-125 — do not batch them together. Hold FEAT-135 (Coursera) out of every batch until Arshad confirms reconnect.

**2026-09-14 batch outcome (run `wf_f057305c-e52`): ALL 12 features halted or errored on the first pass — nothing shipped cleanly.** Full working tree from every agent (unreviewed, spans all 12 features) was preserved on `dev-team/feat-143-155-halted-wip` — **DO NOT MERGE this branch**, it contains a known-critical unfixed finding. Root cause of the 3 "error" results: my own mistake launching the batch — I keyed the features array `id` instead of `featId`, which crashed the script's branch-naming step (`f.featId.toLowerCase()`, no fallback) only for features that had actually reached SHIP. Fixed the script (added an `id`→`featId` fallback) and resumed via `resumeFromRunId` to recover FEAT-148/153/155 from cache rather than re-running the whole pipeline.

| FEAT-156 | GitHub/Gmail/Calendar/Drive/Tasks/YouTube "Connect" bounce-back fix. (Originally FEAT-143, renumbered — see ID collision note above.) | halted | — | — | Architecture Critic BLOCKER (both after 2 redesign rounds): the proposed `/api/v1/auth/{provider}/attach` endpoint is unauthenticated and identity comes solely from a URL-borne ticket — an attacker who mints a ticket for their own account and lures the victim to the URL within 120s captures the victim's provider tokens into the attacker's account. Also a real `status()` signature break across 6 call sites (github.py/gmail.py/google_calendar.py/google_tasks.py/google_drive.py/google_youtube.py — TypeError) and a pre-existing `NameError` in `_shared.py` (missing `timedelta`/`EnqueuedResult` imports) that already breaks every personal-integration "Sync now" **today**, independent of this feature. Needs Arshad's review — the attach-flow design needs a real fix (stash bundle in Redis under a one-time ticket, authenticated completion endpoint), not a retry as-is. |
| FEAT-157 | Calendar/Gmail/GitHub "Sync now" false-success fix. (Originally FEAT-144, renumbered — see ID collision note above.) | halted | — | — | Security Auditor CRITICAL escalation (SEC-001): **confirms a live, currently-deployed production vulnerability**, not something this feature introduced — `_find_user_integration` matches `user_id IS NULL` (shared/project-scoped) rows for ANY authenticated user, and there is no login allowlist anywhere in `backend/src/auth/`. Any stranger who completes Google/GitHub OAuth gets a full account and can read/sync/disconnect the deployment's shared Stripe/Cloudflare/Render/Vercel/Supabase/Anthropic-admin-key integrations. **Verified independently against `origin/claude/ai-personal-assistant-main` (the actual deployed branch) — this gap is real and live in production right now, not just in unmerged pipeline code.** **FIXED 2026-09-14 directly (not via pipeline) — see FEAT-158 below.** |
| FEAT-145 | `disconnect()` should actually revoke credentials. | halted | — | — | Security Auditor SEC-003 (High, not fully fixed): same root cause as FEAT-157's SEC-001 — confirms this feature's own fix (hard-deleting credentials on disconnect) makes the pre-existing authorization gap's impact *worse* (irreversible credential destruction instead of a recoverable status flip) until the login allowlist / ownership check lands first. One clean fix was applied and is safe in isolation: Apple Health's cached biometric snapshot now gets purged from Redis on disconnect (was leaking into backup windows after "delete" was promised) — worth cherry-picking once the branch is otherwise reviewed. |
| FEAT-146 | Slack bare-`Exception` → `IntegrationError` fix. | halted | — | — | Security Auditor CRITICAL (SEC-001, same underlying gap as FEAT-157/145) plus a real SEC-002 finding (High): multiple integrations (OpenWeatherMap, Stack Overflow, and 6+ project providers) leak plaintext API keys/tokens into error messages returned to the client and persisted in Postgres when the upstream call fails (`httpx.HTTPStatusError`'s `str()` includes the request URL with the key in the query string). A `safe_detail()` helper and regression tests were built for this — worth landing once reviewed, independent of the auth-gap fix. |
| FEAT-158 | **Emergency security fix**, done directly (not via the dev-team pipeline — touches `backend/src/auth/`, on the denylist) per Arshad's explicit "Do this" 2026-09-14: closes the FEAT-157/145/146 authorization gap. New `backend/src/auth/allowlist.py` (`AUTH_ALLOWED_EMAILS`, comma-separated); `auth/routers.py`'s `_handle_callback` now 403s `email_not_allowed` for any email not on the allowlist, before `upsert_user_from_oauth` can create a session; `main.py` fails startup closed if `AUTH_ALLOWED_EMAILS` is unset in production (`BACKEND_URL` starts with `https`), matching the existing `SECRET_KEY` fail-fast convention; `integrations/routers.py` adds `_require_owner()` gating `connect` (when `provider.kind == "project_apikey"`), `sync`, and `disconnect` on shared (`user_id IS NULL`) integrations to the allowlisted owner only, as defense-in-depth on top of the login gate. `AUTH_ALLOWED_EMAILS=m.arshadgani@gmail.com` set on Render (`srv-d7m9kub7uimc73cq9afg`) before this merges, so the fail-closed startup check doesn't break the deploy. | in_progress | `claude/ai-personal-assistant-CcA11` | — | Implementation done, tests/gate pending as of this queue-file update. |
| FEAT-147 | Dashboard live-data completion. | halted | — | — | Architecture Critic BLOCKER after 2 rounds: real regressions against the reused FEAT-136/137 branch code (a Decisions-widget predicate that would match the entire inbox instead of just flagged threads; an Alembic-immutability violation — app code importing from a migration file; ~1500 lines of tests that don't match the actual response shape). Needs the SDD reworked against the actual reused branch content, not a blind rebuild. |
| FEAT-148 | `DomainPage.tsx` dead button fix. | in_progress | — | — | Passed the full pipeline (EA: SHIP) on the first run but crashed at the final branch/commit step on my own script bug (see above) before it could be pushed. Recovering via resume — check `tasks/pipeline-runs.md` and this file after the resume run settles. |
| FEAT-149 | Home & IoT/Learning/Travel "Coming soon" relabel. | **completed** | `claude/ai-personal-assistant-CcA11` (direct commit `23681dd`) | SHIP | Actually landed cleanly. Security Auditor flagged one ship-blocking *consistency* issue (not a vulnerability): the pipeline's in-memory code bundle carried two incompatible generations of `ComingSoonPage` (a `label`-prop directory-based version and a `title`-prop flat-file version); the version that reached disk (flat files, `title` prop) is the correct, self-consistent one — verified directly: `tsc --noEmit` clean, 7/7 relevant tests pass. Pushed straight to the active dev branch 2026-09-14 (small, isolated, fully verified — didn't need a separate PR branch). Two non-blocking follow-ups noted for later: bump `react-router-dom`/`@remix-run/router` past a moderate open-redirect CVE (GHSA-2j2x-hqr9-3h42, pre-existing, unrelated to this diff), and strip the now-unused fabricated `home`/`learning`/`travel` KPI seed data from `seed_from_mock.py` + `mockData.ts` so the API itself stops serving fake data even though no page renders it anymore. |
| FEAT-151 | `FundFlowMap` honest-labelling fix. | halted | — | — | Security Auditor escalation — need to pull full detail before re-running; the `dangerouslySetInnerHTML` sink itself was found secure (branded `trustedStaticSvg` type, zero interpolation). |
| FEAT-152 | TopBar dead-control fix. | halted | — | — | Architecture Critic BLOCKER: SDD proposed creating `frontend/src/pages/Settings.tsx` as a new flat file, but `frontend/src/pages/Settings/` already exists as a full directory module (built by the FEAT-153 agent running concurrently in the same batch, same run) — a same-run collision the SDD didn't account for. Simple to fix on retry: point the SDD at the existing directory module instead of proposing a duplicate. |
| FEAT-153 | Sidebar dead-link fix (Settings/Activity log routes). | in_progress | — | — | Passed the full pipeline (EA: SHIP) on the first run, crashed at the same script bug as FEAT-148. Recovering via resume. |
| FEAT-154 | `register_skills.py` auto-invocation. | halted | — | — | Security Auditor escalation — need to pull full detail before re-running. |
| FEAT-155 | Shopify/Plaid hygiene batch. | in_progress | — | — | Passed the full pipeline (EA: SHIP) on the first run, crashed at the same script bug as FEAT-148/153. Recovering via resume. |

**🚨 Standalone critical finding, independent of any of the 13 tickets above — confirmed live in production 2026-09-14:**
`backend/src/integrations/routers.py`'s `_find_user_integration()` (on `origin/claude/ai-personal-assistant-main`, the actually-deployed branch — verified directly, not just in pipeline output) matches `Integration.user_id.is_(None)` for every authenticated user, by design, to let one user's session read/sync/disconnect "shared, project-scoped" integrations (Stripe, Cloudflare, Render, Vercel, Supabase, the Anthropic admin key, Sentry, Upstash, Slack). There is **no login allowlist anywhere in `backend/src/auth/`** on the deployed branch — `upsert_user_from_oauth` creates a full `User` row for any Google or GitHub account that completes OAuth consent, no invite/admin/owner concept exists at all. Net effect, confirmed against the actual deployed route bodies: **any stranger with a Google or GitHub account can sign in to arshad-ai.onrender.com today and call `POST /api/v1/integrations/{slug}/disconnect` or `GET /api/v1/integrations/{slug}/status` (and very plausibly `POST /{slug}/connect` to overwrite the stored key) against every one of those shared provider integrations.** Today's `disconnect()` default only flips a status flag rather than deleting key material (confirmed less catastrophic than after FEAT-145's fix lands), but is still a real, exploitable, unauthenticated-in-practice DoS against production infrastructure integrations, discovered independently by 3 separate Security Auditor runs across FEAT-157/145/146. **FIXED 2026-09-14 — see FEAT-158.**

**Explicitly NOT auto-queued — flagged for Arshad's direct decision, not folded into this batch:** adding a human-confirmation gate before Claude executes real calendar/email/GitHub actions from chat (`backend/src/services/chat.py`'s `_dispatch_tool`) is a product/safety decision, not a bug — currently there is no confirmation step before a chat-triggered tool call takes a real action. Needs Arshad's explicit sign-off on the desired UX (confirm-every-time vs. confirm-first-time-per-action-type vs. none) before it becomes a FEAT ticket.

**Permanently excluded — nothing to add regardless of approval, because no concrete feature was ever proposed for these (a technical/legitimacy impossibility, not a value judgment the "approve all" instruction can override):**
- **PayPal, ProfileMaster** — Arshad explicitly said ignore.
- **Uber Eats** — the connector's tool surface is `search` + `publish_analytics` only; there is no order-history or order-status tool, so it structurally cannot provide the personal data any feature here would need.
- **Booking.com as a personal-data source** — the connector is authless (no identity), exposing only public accommodation/attraction search; it cannot return Arshad's own bookings. (Its one legitimate idea, trip-feasibility checking using Calendar/Shopify/Render data instead, is already FEAT-129.)
- **Lusha** — B2B contact/company enrichment; the analysis found no legitimate personal-life-dashboard use case, and force-fitting one would mean running third-party enrichment against people (e.g. store customers) who never consented to it.

Everything else that was ever a real proposal is now queued somewhere above — Spotify's idea folded into FEAT-121, Zomato's "burnout index" folded into FEAT-126, and Coursera moved from excluded to queued-but-blocked as FEAT-135.

**Important — concurrency constraint on launching:** Two or more features can only share the "same specialist role never runs concurrently" guarantee if they run inside the SAME `Workflow()` invocation (the `locks` object is local to one script execution, not shared across separate calls). Never start a second `Workflow` run while another is still active — wait for it to settle, then resume/restart with the full set of pending features in one `args.features` array.

**Known gap:** only 9 of the 28 roles CLAUDE.md documents have actual `.claude/agents/dev-team/*.md` files (`bug-fixer`, `business-analyst`, `developer`, `enterprise-architect`, `orchestrator`, `process-organiser`, `solution-architect`, `test-script-writer`, `tester`). The other ~19 conceptual roles have no dev-team-specific agent — the workflow script maps them to the closest existing agent in the full roster instead (e.g. `system-engineer` → `system-architect`, `senior-engineer` → `code-analyzer`). If a future stage errors with "agent type not found," check this mapping first before assuming it's a new bug.

| FEAT-143 | UI Design Council: run all UI screens (Login, AuthCallback, Dashboard, Chat, PersonalFinance, ShopifyStore, StockMarket, HealthFitness, Learning, HomeIoT, Travel, Integrations, AiEcosystem, Obsidian) against ui-ux-pro-max, impeccable, taste-skill, and design-motion-principles skills; a 4-auditor + 1-council-chair panel decides style-only fixes (CSS Modules / tokens.css only, zero feature/structure changes); apply approved changes and verify with tsc + existing test suite. | completed | `claude/ui-repos-reference-jusj4c` | approved | **Not run via the standard 28-agent dev-team-pipeline** — custom `Workflow` script (`ui-design-council`, run `wf_1c32197c-74b`) per Arshad's explicit request for a "council" mechanism: 4 parallel audit agents + 1 council-chair synthesizing style-only, token-consistent decisions (45 approved changes across 18 files, 2 rejected as redundant/out-of-scope-for-a-mechanical-fix). Biggest finding: Integrations, AiEcosystem, and Obsidian pages referenced nonexistent CSS custom properties (`--text`, `--surface`, `--border`, `--bg`, `--accent-hover`, `--error`) that silently fell back to hardcoded GitHub-dark colors instead of the app's real cyan HUD theme — now repointed to real tokens. Also fixed: WCAG AA contrast failure on `--text-muted` (app-wide), several sub-44px touch targets (CRITICAL a11y), missing `prefers-reduced-motion` guards on AppleHealthCard's pulse/shimmer animations, off-scale border-radii, and a malformed `box-shadow` value on AiEcosystem cards that silently discarded the hover glow. Zero JSX/structural/feature changes — verified via `tsc --noEmit` (no new errors) and `npm test` (36/36 files, 273/273 tests passing, including the orphans/overflow CSS structural tests). **Merged to `claude/ai-personal-assistant-main` via PR #86** (squash `96030cc`) after the 8-agent "Merge to Main" gate (WARN verdict, 0 FAIL/Critical — see `tasks/last-gate-report.md`). Gate surfaced a follow-up checklist of narrower findings not in the original council scope — see FEAT-144. |
| FEAT-144 | Follow-up from FEAT-143's Merge-to-Main gate report — 8 items, all style-only or test-only (see full item list in the previous row of this file's git history, or `tasks/last-gate-report.md`'s Action Items section). | completed | `claude/ui-repos-reference-jusj4c` | — | All 8 items applied directly (not via Workflow — small, well-scoped follow-up list). Item 2 (Opus/Sonnet distinction) resolved per Arshad's explicit choice: weight/opacity tiers (Opus solid `--accent` fill, Sonnet `--accent-faint`/`--accent-soft`). Added `frontend/src/styles/tokens.test.ts` (item 5) — a static guard that failed on exactly the 10 leftover sites from item 1 before the fix landed, confirming both the bug and the fix. Verified: `tsc --noEmit` clean, `npm test` 37/37 files, 274/274 tests (273 existing + the new guard). Pushed (`ebfc6da`). Not yet merged to `claude/ai-personal-assistant-main` — needs its own Merge-to-Main gate cycle. |

## Completed

FEAT-118, FEAT-119, and FEAT-120 are completed (see their rows in the Queue
table above for branch/EA-decision detail — kept in place rather than moved
here, since other rows' notes cross-reference them by FEAT_ID, e.g. FEAT-125
and FEAT-129's dependency notes). All three merged to
`claude/ai-personal-assistant-main` via PR #71 (squash `b348e79`,
2026-09-12) and are confirmed deployed and healthy.
