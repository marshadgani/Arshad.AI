# PipelineApp

**Domain:** `data-pipeline`

Airflow DAG status viewer, ingestion logs, and pipeline health dashboard.

## Structure

```
applications/
├── src/      ← React components / FastAPI routes
├── tests/    ← component and integration tests
├── config/   ← environment configuration
└── README.md
```
