# ChatApp

**Domain:** `ai-core`

Main chat interface connecting the user to the Claude orchestrator.

## Structure

```
applications/
├── src/      ← React components / FastAPI routes
├── tests/    ← component and integration tests
├── config/   ← environment configuration
└── README.md
```
